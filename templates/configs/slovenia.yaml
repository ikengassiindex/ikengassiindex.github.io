schema_version: "1.0"

# === Country identity (used across all 8 page types) ===
slug: slovenia
country_name: Slovenia
country_possessive: Slovenia's
fleet_count: 158

# === Administrative level (used by all pages) ===
admin:
  count: 12
  level_label_singular: "NUTS-3 Region"
  level_label_plural: "NUTS-3 Regions"
  short: "NUTS-3"

# === Edition (used by intelligence + esg-report) ===
edition:
  number: "01"
  month: "July 2026"
  inaugural: true
  subtitle: "The Savinja Valley Corridor"

# === Page-specific content (rich narrative only for narrative-heavy pages) ===

# Map page — minimal config (page is structurally uniform)
map: {}

# (intelligence/esg-report/data/methodology/index/regional/dno-dashboard configs
#  will be added as those page templates are built)
