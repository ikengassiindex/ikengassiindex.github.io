# SSI Pipeline — Dev / Test Dependencies
# Install in addition to requirements.txt for running tests locally + in CI.
#
# Usage:
#   pip install -r scripts/pipeline/requirements.txt
#   pip install -r scripts/pipeline/requirements-dev.txt
#   pytest tests/

# Production dependencies (re-included so a single `pip install -r requirements-dev.txt`
# brings up a complete dev environment)
-r requirements.txt

# Test runner
pytest>=7.0,<9.0
pytest-cov>=4.0           # Coverage reporting
